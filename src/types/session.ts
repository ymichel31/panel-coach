export type SessionInput = {
  title: string;
  date: string;
  description: string;
};

export type Session = {
  id: string;
  title: string;
  date: string;
  description: string;
  created_at: string;
  updated_at: string;
};