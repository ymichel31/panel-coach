export type SkillInput = {
  name: string;
  category: string;
  maxScore: number;
};

export type Skill = {
  id: string;
  name: string;
  category: string;
  max_score: number;
  created_at: string;
  updated_at: string;
};